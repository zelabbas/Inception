server {
    listen 127.0.0.2:80;  # This will listen on IP 127.0.0.2 on port 80
    server_name localhost2;

    root /var/www/html;
    index index.html index.htm;

    location / {
        try_files $uri $uri/ =404;
    }
}